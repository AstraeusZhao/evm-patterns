# Whitelist

An owner-managed allowlist plus a gated sale demonstration.

## Properties

- Add/remove is owner-only and idempotence is guarded.
- `onlyWhitelisted` gates downstream functions; the sale contract rejects
  purchases from unlisted accounts.
